# Authors configure ae/reproduce.local.env on the GPU server before granting access.
# The GPU server needs SSH/rsync access to the phone server.
PHONE_HOST=reviewer@phone-server
PHONE_REPO=/path/to/AutoTailor-AE
PHONE_RESULT_ROOT=/path/to/AutoTailor-AE-results
